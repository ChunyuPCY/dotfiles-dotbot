# 常用快捷方式
alias zknew 'zk new' # 新建笔记:  zknew --title "标题"（固定创建到笔记库）
alias zkl 'zk list' # 列出笔记:  zkl -t 标签 / -m 关键词
alias zkf 'zk edit --interactive' # fzf 模糊搜索并编辑（最高频）
alias zkg 'zk graph -f json > graph.json' # 导出关系图
alias zkt 'zk tag list' # 查看所有标签
