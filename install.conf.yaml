- defaults:
    link:
      create: true
      force: true
      relink: true

- clean: ["~"]

- link:
    ~/.config/starship: ./starship

- shell:
    - git submodule sync --recursive
    - git submodule update --init --recursive --remote
